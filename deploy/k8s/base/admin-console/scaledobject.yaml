apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: admin-console
spec:
  scaleTargetRef:
    name: admin-console
  minReplicaCount: 2
  maxReplicaCount: 4
  triggers:
    - type: cpu
      metricType: Utilization
      metadata:
        value: "80"
